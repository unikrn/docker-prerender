prerender:
    build: ./prerender
    security_opt: 
       - seccomp:chrome.json
    restart: always
nginx:
    restart: always
    build: ./nginx
    volumes:
       - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "3000:8080"
    links:
        - prerender
